using UnityEngine;
using System.Collections;

public enum LocaleType
{
    Korean,
    English
}
public static class LocaleManager
{
    //일단 여기에 코드로 때려박고, 나중에 파일로 빼던가 하자

    public static string Get(string key)
    {
        return string.Empty;
    }
}